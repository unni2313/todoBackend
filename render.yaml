services:
  - type: web
    name: todo-backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: 5000
